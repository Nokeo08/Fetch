{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun --watch run src/server.ts",
    "start": "bun run src/server.ts"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "hono": "^4.10.8",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
